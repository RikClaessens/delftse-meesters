import React from 'react';

import { Logo } from '../components';

const ComingSoon = () => (
  <div
    style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      height: '100vh',
      width: '100vw',
    }}
  >
    <Logo />
  </div>
);

export default ComingSoon;
